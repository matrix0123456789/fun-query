name: PHP Tests

on:
  push:

  pull_request:


permissions:
  contents: read

jobs:
  test74:
    runs-on: ubuntu-latest
    steps:
      - env:
        - ACTION_PHP_VERSION: '7.4'
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress
      - name: Run test suite
        run: composer run-script test
  test80:
    runs-on: ubuntu-latest
    steps:
      - env:
          - ACTION_PHP_VERSION: '8.0'
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress
      - name: Run test suite
        run: composer run-script test
  test81:
    runs-on: ubuntu-latest
    steps:
      - env:
          - ACTION_PHP_VERSION: '8.1'
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress
      - name: Run test suite
        run: composer run-script test
  test82:
    runs-on: ubuntu-latest
    steps:
      - env:
          - ACTION_PHP_VERSION: '8.2'
      - name: Install dependencies
        run: composer install --prefer-dist --no-progress
      - name: Run test suite
        run: composer run-script test
      
